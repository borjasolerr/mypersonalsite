{% for navitem in mainnav[locale] %}
  {% if loop.first %}<ul>{% endif %}
    <li class="c-lggnav__item"><a href="{{ navitem.url }}">{{ navitem.label }}</a></li>
  {% if loop.last %}</ul>{% endif %}
{% endfor %}
